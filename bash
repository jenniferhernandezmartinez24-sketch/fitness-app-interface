# Create local project directory
mkdir fitness-app-interface
cd fitness-app-interface

# Initialize Git
git init
git branch -M main

# Add remote
git remote add origin https://github.com/jenniferhernandezmartinez24-sketch/fitness-app-interface.git

# Create the files (copy the code blocks above into your local directory structure)
# Then commit and push
git add .
git commit -m "Initial commit: Complete fitness app interface with React and Tailwind"
git push -u origin main
